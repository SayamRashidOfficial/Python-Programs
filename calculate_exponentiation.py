def exponentiation_operator(base, exponent):
    return base ** exponent

base = 2
exponent = 3
result = exponentiation_operator(base, exponent)
print(f"{base} raised to the power of {exponent} is: {result}")
